name: Release

on:
  push:
    tags:
      - '*'

jobs:
  build:
    name: Build for ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            binary_name: mcp2cli-linux-x86_64
            use_cross: false
          - os: ubuntu-latest
            target: aarch64-unknown-linux-gnu
            binary_name: mcp2cli-linux-arm64
            use_cross: true
          - os: macos-latest
            target: x86_64-apple-darwin
            binary_name: mcp2cli-macos-x86_64
            use_cross: false
          - os: macos-latest
            target: aarch64-apple-darwin
            binary_name: mcp2cli-macos-arm64
            use_cross: false
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            binary_name: mcp2cli-windows-x86_64.exe
            use_cross: false
          - os: windows-latest
            target: aarch64-pc-windows-msvc
            binary_name: mcp2cli-windows-arm64.exe
            use_cross: false

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install Cross
        if: matrix.use_cross
        uses: taiki-e/install-action@cross

      - name: Build
        run: |
          if [ "${{ matrix.use_cross }}" = "true" ]; then
            cross build --release --target ${{ matrix.target }}
          else
            cargo build --release --target ${{ matrix.target }}
          fi
        env:
          APP_RELEASE_VERSION: ${{ github.ref_name }}
          CROSS_CONTAINER_ENV_PASSTHROUGH: APP_RELEASE_VERSION

      - name: Rename binary
        shell: bash
        run: |
          if [[ "${{ matrix.target }}" == *"windows"* ]]; then
            mv "target/${{ matrix.target }}/release/mcp2cli.exe" "${{ matrix.binary_name }}"
          else
            mv "target/${{ matrix.target }}/release/mcp2cli" "${{ matrix.binary_name }}"
          fi

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.binary_name }}
          path: ${{ matrix.binary_name }}

  release:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/*
          body: "Release ${{ github.ref_name }}"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
